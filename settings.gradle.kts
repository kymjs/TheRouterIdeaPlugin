rootProject.name = "TheRouterIdeaPlugin"
pluginManagement {
    repositories {
        maven("https://maven.therouter.cn:8443/repository/maven-public/")
        gradlePluginPortal()
        mavenCentral()
    }
}